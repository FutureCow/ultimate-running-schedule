[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # python-jose calls the deprecated datetime.utcnow() internally
    ignore:datetime.datetime.utcnow\(\) is deprecated:DeprecationWarning:jose.*
